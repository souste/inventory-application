<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Games</title>
  </head>
  <body>
    <h1>Games</h1>
    <a href="/games/create">New Game</a>
    <ul>
      <% for (let i = 0; i < games.length; i++) { %>
      <li>
        <h3><%= games[i].name %></h3>
        <p>Length: <%= games[i].length %> hours</p>
        <p>Price: £<%= games[i].price %></p>
        <a href="/games/<%= games[i].id %>"><button>Open</button></a>
      </li>
      <% } %>
    </ul>
    <a href="/"><button>Back</button></a>
  </body>
</html>
