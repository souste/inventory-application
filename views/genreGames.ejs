<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Games in Single Genre</title>
  </head>
  <body>
    <ul>
      <% for (let i=0; i < games.length; i++) { %>
      <li>
        <h3><%= games[i].name %></h3>
        <p>Length: <%= games[i].length %> hours</p>
        <p>Price: £<%= games[i].price %></p>
        <a href="/games/<%= games[i].id %>"><button>Open</button></a>
      </li>

      <% } %>
      <a href="/genres"><button>Back</button></a>
    </ul>
  </body>
</html>
